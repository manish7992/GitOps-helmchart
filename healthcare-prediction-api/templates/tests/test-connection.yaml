apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "healthcare-prediction-api.fullname" . }}-test-connection"
  labels:
    {{- include "healthcare-prediction-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "healthcare-prediction-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
